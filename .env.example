# Proyecto de Supabase. Settings → API keys.
NEXT_PUBLIC_SUPABASE_URL=https://fbfpkdrdlxxilezxsara.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Llave secreta. Solo para los scripts de carga y sembrado, nunca en el navegador.
SUPABASE_SECRET_KEY=

# Cadena de conexión directa, opcional. Settings → Database → Connection string → URI.
# Solo hace falta si quieres aplicar el esquema desde la línea de comandos en lugar del
# editor SQL de Supabase.
SUPABASE_DB_URL=
